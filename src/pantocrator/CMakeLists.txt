set(EXAMPLE_NAME pantocrator)
set(HEADER_PATH ${CMAKE_SOURCE_DIR}/include/${EXAMPLE_NAME})
FILE(GLOB HEADERS "${HEADER_PATH}/*.h*")
FILE(GLOB SOURCES "*.cpp")

include_directories(
	${CMAKE_SOURCE_DIR}/include
	${OSG_INCLUDE_DIR}
    	${QT_INCLUDE_DIR}
)

SET(LIBRARIES_OPTIMIZED osgParti )
SET(LIBRARIES_DEBUG osgParti )

IF   (QT4_FOUND)
    SET(LIBRARIES_OPTIMIZED 
	${LIBRARIES_OPTIMIZED} 
	${QT_QTCORE_LIBRARY_RELEASE} 
	${QT_QTGUI_LIBRARY_RELEASE} 
	${QT_QTOPENGL_LIBRARY_RELEASE} )
    ADD_DEFINITIONS(-DUSE_QT4)
ELSE(QT4_FOUND)
    SET(LIBRARIES_DEBUG 
	${LIBRARIES_DEBUG} 
	${QT_QTCORE_LIBRARY_RELEASE} 
	${QT_QTGUI_LIBRARY_RELEASE} 
	${QT_QTOPENGL_LIBRARY_RELEASE} )
ENDIF(QT4_FOUND)



SETUP_CONSOLE_EXAMPLE(${EXAMPLE_NAME})

